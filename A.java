package oops;

public class A {
     int num;

    public A(int num) {
        this.num = num;
    }

    public void display() {
        System.out.println("Number in class A: " + num);
    }
}


























//
//This file defines a class named A, which is declared within the oops package.
//The class A has a single instance variable num of type int, 
//There's a constructor A(int num) that initializes the num variable with the value passed as an argument.
//The class A contains a method display() which simply prints the value of num to the console.



//which is declared as protected. This means it can be accessed by subclasses within the same package.